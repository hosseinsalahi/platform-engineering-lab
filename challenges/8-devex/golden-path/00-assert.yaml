# Phase 1: the quota lands in the team's namespace
apiVersion: v1
kind: ResourceQuota
metadata:
  name: golden-path-quota
  namespace: cnpe-devex-team
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 4Gi
    pods: "20"
