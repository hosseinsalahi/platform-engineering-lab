# The developer's namespaced composite resource exists.
# Crossplane v2 has no claims - the XR is created directly in the namespace.
apiVersion: kuttl.dev/v1beta1
kind: TestAssert
commands:
  - script: kubectl get xdatabaserequest test-db -n cnpe-selfservice-test
