# Phase 1: Policy rule is in Enforce mode
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: require-memory-limits
spec:
  rules:
  - name: require-memory-limits
    validate:
      failureAction: Enforce
